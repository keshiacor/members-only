<div>
  <h1> All Blog Posts </h1>
  <% @posts.each do |post| %>
    <div>
      <h2><%= post.title %></h2>
      <p><%= post.description %></p>
      <% if user_signed_in? %>
      <h3> Posted by: <%= post.user.username %> </h3>
      <% end %>
    </div>
  <% end %>
  
</div>